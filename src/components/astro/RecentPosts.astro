---
import PostPreview from "./PostPreview.astro";

const { posts } = Astro.props
---

<div class="flex flex-col gap-8 max-w-3xl pb-20">
    {posts.map((p)=>(
        <PostPreview post={p}/>
    ))}
</div>